# Environment taxonomy

Version 1 is exported from the canonical DSR SPL Reference catalogue. IDs, not display labels, are stable identities. Validators reject duplicate IDs and invalid group/type pairs.
